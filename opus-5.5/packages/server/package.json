{
  "name": "@opusdb/server",
  "version": "0.1.0",
  "description": "OpusDB network server: HTTP/JSON API, WebSocket sessions and the PostgreSQL wire protocol",
  "type": "module",
  "bin": {
    "opusdb": "./src/main.ts"
  },
  "scripts": {
    "start": "node src/main.ts",
    "test": "node --test --test-reporter=spec \"test/**/*.test.ts\"",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@opusdb/engine": "0.1.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0"
  }
}
